spring:
  application:
    name: QQ-POS

  # DB Connect
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL_TEST}
    username: ${DB_USERNAME_TEST}
    password: ${DB_PASSWORD_TEST}

qq-pos:
  jwt:
    admin-secret-key: ${JWT_SECRET_KEY_TEST}
    admin-ttl: 7200000
    admin-token-name: token

