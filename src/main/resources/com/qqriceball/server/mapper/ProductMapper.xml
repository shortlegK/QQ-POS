<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qqriceball.server.mapper.ProductMapper">

    <!--新增菜單品項-->
    <insert id="insert" parameterType="com.qqriceball.pojo.entity.Product" useGeneratedKeys="true" keyProperty="id">
        insert into product(title, product_type, price, status, create_id, create_time, update_id, update_time)
        values (#{title},#{productType}, #{price}, #{status} ,#{createId}, #{createTime}, #{updateId}, #{updateTime})
    </insert>

</mapper>