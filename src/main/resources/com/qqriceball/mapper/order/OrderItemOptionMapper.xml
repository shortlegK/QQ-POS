<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qqriceball.mapper.order.OrderItemOptionMapper">


    <!--新增訂單項目-->
    <insert id="insert" parameterType="com.qqriceball.model.entity.order.OrderItemOption" useGeneratedKeys="true" keyProperty="id">
        insert into order_item_options(order_item_id, option_id, option_type, option_title, option_price, quantity)
        values (#{orderItemId},#{optionId},#{optionType},#{optionTitle},#{optionPrice}, #{quantity})
    </insert>

</mapper>