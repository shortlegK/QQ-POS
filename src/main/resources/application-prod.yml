# 正式環境設定
logging:
  file:
    name: logs/qq-pos.log
  level:
    root: info

qq-pos:
  jwt:
    secret-key: ${JWT_SECRET_KEY_PROD}

spring:
  datasource:
    url: ${DB_URL_PROD}
    username: ${DB_USERNAME_PROD}
    password: ${DB_PASSWORD_PROD}